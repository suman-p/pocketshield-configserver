build:
  version: "2.0"

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pocketshield
    username: root
    password: Water123##Water123

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

server:
  port: 8081

